# Python / venv
.venv/
__pycache__/
*.pyc
*.pyo

# pyvespa generated application packages (regenerate with package.to_files())
generated_app/
ezcater-demo/generated_ezcater/

# OS / editor / tooling
.DS_Store
.idea/
.vscode/
*.swp
.claude/

# Maven (pro-java-searcher build output)
target/

# MPS experiment venv
mps-embed/.venv/

# Node / React (ezcater-demo)
node_modules/
ezcater-demo/web/dist/

.env

# ingestion / serving runtime artifacts (regenerated at ingest/query time)
ezcater-demo/data/.enrich_cache.json
ezcater-demo/data/.semcache.json
ezcater-demo/data/ontology_graph.json
ezcater-demo/data/ontology_graph_bkup.json

# Knowledge Graph materialized ABox (derived cache; rebuilt from the TBox + dishes.jsonl)
ezcater-demo/data/kg_graphs/
